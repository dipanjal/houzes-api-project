let VerificationCodeModel = require('./verification-code-model');
// let User = require('../../modules/oauth/sequlizer_models').User;

// VerificationCodeModel.belongsTo(User,{foreignKey: 'user_id'});

module.exports = {
    VerificationCodeModel:VerificationCodeModel
};